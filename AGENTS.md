# 프로젝트 개요

인터넷 쇼핑몰(스포츠 의류) 관리 프로그램 (C++ Term Project)

## 필수 기능

- [ ] 필수기능1: 회원제 운영 (회원/비회원 구분)
- [ ] 필수기능2: 구매이력 기반 포인트/Advantage 기능
- [ ] 필수기능3: 상품 구매 후 환불 기능

## 클래스 구조

> ⚠️ 본 계획은 작업 진행에 따라 수정될 수 있습니다.

### 1. Account (계정)

| 종류 | 이름 | 설명 | 구현 상태 |
|------|------|------|:---------:|
| 변수 | 아이디 | 사용자 ID | ✅ |
| 변수 | 비밀번호 | 사용자 비밀번호 | ✅ |
| 변수 | 적립 포인트 | 누적 포인트 | ☐ |
| 변수 | 쇼핑몰에서 구매한 금액 | 총 구매 금액 | ☐ |
| 변수 | 구매한 옷 목록 | 구매 내역 (`std::vector<Cloth>`) | ☐ |
| 기능 | 아이디 반환 | `get_id()` | ✅ |
| 기능 | 비밀번호 확인 | `check_password_correct()` | ✅ |
| 기능 | 옷 추가 | 구매한 옷 목록에 추가 | ☐ |
| 기능 | 옷 삭제 | 구매한 옷 목록에서 삭제 (환불) | ☐ |
| 기능 | 데이터 반환 | getter 일괄 | ☐ |
| 기능 | 데이터 수정 | setter 일괄 | ☐ |

### 2. AccountManager (계정 관리자)

> `Account`들의 목록을 관리하는 클래스. `Server`가 소유한다.

| 종류 | 이름 | 설명 | 구현 상태 |
|------|------|------|:---------:|
| 변수 | 계정 리스트 | `std::vector<Account>` | ✅ |
| 기능 | 계정 추가 | `add_account_on_list()` | ✅ |
| 기능 | 계정 삭제 | `remove_account_on_list(user_id)` | ☐ |
| 기능 | 계정 생성 (입력) | `create_user_information()` | ✅ |
| 기능 | 아이디 중복 확인 | `find_id_exists(id)` | ✅ |
| 기능 | 계정 목록 출력 | `show_existing_accounts()` | ✅ |

### 3. Cloth (옷)

| 종류 | 이름 | 설명 | 구현 상태 |
|------|------|------|:---------:|
| 변수 | 이름 | 옷 이름(종류) | ☐ |
| 변수 | 색상 | 옷 색상 | ☐ |
| 변수 | 종류 | 상의/하의 등 유형 | ☐ |
| 변수 | 가격 | 옷 가격 | ☐ |
| 변수 | 제품 번호 | 동일 옷 구분용 번호 | ☐ |
| 기능 | 데이터 반환 | getter 일괄 | ☐ |
| 기능 | 데이터 수정 | setter 일괄 | ☐ |

> **제품 번호**: 색상, 종류, 이름 등 모든 것이 똑같은 여러 개의 옷을 구분하기 위한 번호. 옷의 종류, 색상, 유형, 동일한 옷 중 몇 번째인지를 구분할 수 있어야 한다.

### 4. Server (서버 - 중추 관리 클래스)

> `AccountManager`, 재고, 상품 카테고리 등 모든 하위 객체를 소유하고 비즈니스 로직을 총괄하는 중앙 클래스.
> `main()`에서는 `Server` 객체 하나만 생성하여 모든 작업을 처리한다.

| 종류 | 이름 | 설명 | 구현 상태 |
|------|------|------|:---------:|
| 변수 | 계정 관리자 | `AccountManager` (회원 목록 관리 위임) | ☐ |
| 변수 | 재고 목록 | `std::vector<ClothStock>` (실제 재고) | ☐ |
| 변수 | 옷 유형 리스트 | 옷 카테고리 (enum 또는 리스트) | ☐ |
| 변수 | 현재 로그인된 계정 | `Account* logged_in_account` (포인터, 미로그인 시 `nullptr`) | ☐ |
| 기능 | 로그인 | id/password 확인 후 `logged_in_account` 설정 | ☐ |
| 기능 | 로그아웃 | `logged_in_account`를 `nullptr`로 초기화 | ☐ |
| 기능 | 회원가입 | `AccountManager`에 계정 추가 위임 | ☐ |
| 기능 | 옷 구매 | 구매 처리 및 포인트 적립 | ☐ |
| 기능 | 옷 환불 | 환불 처리 및 포인트 회수 | ☐ |
| 기능 | 포인트 적립 | 구매 가격 5만원마다 적립금 1,000원 | ☐ |

### 5. ClothStock 구조체 (재고 관리)

> 같은 옷이 재고가 여러 개 있을 수 있으므로 `struct`로 관리하며 `Server`가 `std::vector<ClothStock>`로 저장한다.

| 필드 | 설명 | 구현 상태 |
|------|------|:---------:|
| 옷 이름 | 옷의 이름 | ☐ |
| 옷 색상 | 색상 정보 | ☐ |
| 옷 유형 | 상의, 하의 등 카테고리 | ☐ |
| 옷 가격 | 가격 | ☐ |

### 6. ConsoleUtil (입출력 유틸리티)

> 콘솔 입출력 관련 보조 함수를 모아둔 네임스페이스.

| 종류 | 이름 | 설명 | 구현 상태 |
|------|------|------|:---------:|
| 기능 | 오프닝 출력 | `opening()` - 프로그램 시작 메시지 | ✅ |
| 기능 | 구분선 반환 | `get_divider()` | ✅ |

## 비즈니스 규칙

- **포인트 적립**: 구매 가격 5만원마다 적립금 1,000원
- **회원/비회원**: 회원제로 운영되며, 회원과 비회원의 처리 방식을 구분해야 함
  - 비회원: 로그인 없이 구매 가능 (구매 이력/포인트 적립 없음)
  - 회원: 로그인 후 구매, 포인트 적립 및 사용 가능

## 네이밍 규칙

- **클래스 / 구조체 / enum**: `PascalCase` (예: `Account`, `Cloth`, `ColorType`)
- **함수**: `snake_case` (예: `process_order()`, `get_name()`)
- **변수 (지역변수, 매개변수)**: `snake_case` (예: `user_name`, `total_count`)
- **멤버변수**: `snake_case`, 접두사 없음 (예: `name`, `points`)
  - 멤버변수와 지역변수/매개변수 구분이 필요할 때는 `this->` 사용
- **전역변수**: `snake_case`, 가능하면 사용 지양 (예: `divider`)
  - 전역변수가 꼭 필요한 경우 `inline` 또는 `extern`으로 관리
- **상수**: `snake_case` (예: `max_size`)
- **매크로**: `ALL_CAPS` + `snake_case` (예: `DEBUG_MODE`)
